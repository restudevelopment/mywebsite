AOS.init({
    duration: 1200,  // Durasi animasi (ms)
    easing: "ease-in-out", // Efek transisi
    once: false,  // Animasi akan berjalan setiap kali elemen masuk viewport
    mirror: true, // Animasi tetap berjalan saat scroll ke atas lagi
    anchorPlacement: "top-bottom", // Memicu animasi saat elemen masuk viewport
});
